In the parent folder(parallel to setup.py) run the below command:

python setup.py sdist --formats=gztar


That will create a folder and will create a file called trainer-0.1.tar.gz. The file name and the version coming from setup.py

Move above .gz file to a bucket location. In the training, use trainer.task as the python environment. 


